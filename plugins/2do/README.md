# 2Do Tools Plugin

This plugin registers the `2do` MCP server with clients that support plugin
marketplaces.

Install the `2do-tools` command first, then install this plugin from the
marketplace. The plugin intentionally uses the plain `2do-tools` command so each
client can run the server from the user's normal install location.
